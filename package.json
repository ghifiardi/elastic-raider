{
  "name": "elastic-raider",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "serve": "node --version >/dev/null && python3 -m http.server 8080"
  }
}
